# Password for logging into the cloud sync (the only account credential)
AUTH_PASSWORD=change-me

# Postgres password used inside the compose network (optional, defaults to drawdb)
#POSTGRES_PASSWORD=drawdb
